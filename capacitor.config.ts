import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'test_rendering',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
